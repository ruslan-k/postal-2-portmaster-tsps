Place the original Linux Postal 2 1409 game data here, or put the supplied source archive next to this directory as `Postal 2.zip` and run the port once.

The setup script expects this layout inside the archive:
  postal2/gamedata/System/postal2-bin

Game data is intentionally not included in the repository or generated PortMaster package.
